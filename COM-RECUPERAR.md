# Com recuperar xavibosch-profile-ascii al Mac

## 1. Baixar-lo

```bash
cd ~/Developer
git clone https://github.com/xavibosch/xavibosch.git
cd xavibosch
```

## 2. Deixar-lo a punt

### Obrir-lo
Aquest projecte no necessita instal·lar res: obre la carpeta i llest.

## 3. Guardar els canvis que facis

```bash
git add -A
git commit -m "què has canviat"
git push
```

## Què NO hi ha en aquest repositori

Dependències (`node_modules`, `.venv`), compilats (`build`, `dist`, DerivedData) i fitxers de claus (`.env`). Tot això es torna a generar amb els passos de dalt; les claus les has de tornar a posar tu.

---
Còpia de seguretat creada el 23/09/2026. Original al Mac: `~/Developer/PROTOFLIO VERSIONS/xavibosch-profile-ascii`
